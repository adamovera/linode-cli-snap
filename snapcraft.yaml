---
name: linode-cli
title: Linode CLI
version: "5.63.0"
license: MIT
summary: "Manage your Akamai Cloud resources from the terminal."
description: |
  The [Linode CLI](https://github.com/linode/linode-cli) provides full command-line access to your [Akamai Cloud](https://www.linode.com) account through the Linode API. Almost any action available in Cloud Manager can also be performed directly from the terminal.

  ## Features
  - **Compute**: Create, update, and delete Linodes, manage instance types, images, backups, and power states.
  - **Networking**: Manage IP addresses, VLANs, VPCs, Firewalls, and DNS zones and records.
  - **Storage**: Provision and manage Block Storage volumes and Object Storage buckets.
  - **Load Balancing**: Configure and monitor NodeBalancers and backend nodes for high availability.
  - **Databases**: Deploy and maintain Managed Databases for MySQL and PostgreSQL.
  - **Kubernetes**: Create, update, and manage Linode Kubernetes Engine (LKE) clusters.
  - **Events**: List and inspect system and account events for auditing and automation.
  - **Account**: View billing info, manage users, SSH keys, API tokens, and account settings.
  - **Support**: Open and track support tickets.
  - **Automation**: Execute StackScripts, upload custom images, and integrate with CI/CD workflows.

  ## Availability
  The Linode CLI is available for both **amd64** and **arm64** architectures and can be used across [all Akamai Cloud regions](https://www.linode.com/global-infrastructure/).

  ## Usage
  Installation:

  ```bash
  snap install linode-cli
  ```

  Configuration:

  ```bash
  linode-cli configure
  ```

  Help:

  ```bash
  linode-cli --help
  ```
base: core24
grade: stable
confinement: strict
source-code:
  # - https://github.com/linode/linode-cli
  - https://github.com/adamovera/linode-cli-snap
contact:
  # - https://www.linode.com/support/
  - mail@adamovera.com
website:
  # - https://techdocs.akamai.com/cloud-computing/docs/cli
  - https://adamovera.com
issues:
  # - https://github.com/linode/linode-cli/issues
  - https://github.com/adamovera/linode-cli-snap/issues
icon: icon.png

platforms:
  amd64:
    build-on: [amd64]
    build-for: [amd64]
  arm64:
    build-on: [arm64]
    build-for: [arm64]

apps:
  linode-cli:
    command: bin/linode-cli
    aliases:
      - linode
      - lin
    plugs:
      - home
      - network

parts:
  linode-cli:
    plugin: python
    source: .
    python-packages:
      - linode-cli==$SNAPCRAFT_PROJECT_VERSION
